import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

// Tidak perlu import './index.css' kalau tidak punya

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
